<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CAS.AgileWorkloadTracker.SiteManagement" x:Class="CAS.AgileWorkloadTracker.SiteManagement.MainWindow"
        Title="MainWindow" Height="350" Width="525">
	<Grid x:Name="x_MainGrid">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.DataContext>
			<local:MainWindowData/>
		</Grid.DataContext>
		<Label Content="Site URL" Margin="0" VerticalAlignment="Center" Padding="1"/>
		<TextBox x:Name="x_SiteURLTextBox" Grid.Column="1" Margin="0" TextWrapping="Wrap" Text="URL" VerticalAlignment="Top" DataContext="{Binding SiteURL, Mode=TwoWay, TargetNullValue=Enter site URL, UpdateSourceTrigger=LostFocus}" ScrollViewer.CanContentScroll="True" VerticalContentAlignment="Center"/>
		<Button x:Name="x_RefreshButton" Content="Refresh" Grid.Column="2" Margin="0" VerticalAlignment="Top" Width="75" Click="x_RefreshButton_Click"/>
	</Grid>
</Window>
